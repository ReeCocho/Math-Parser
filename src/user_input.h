/**
 * Utilities for reading user input as string.
 */
 
/**
 * Read a line from the user.
 * @return String containing user input.
 * @note The string is dynamically allocated using malloc, and should be freed.
 */
extern char* mp_get_user_input(void);